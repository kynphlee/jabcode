/**
 * Mobile utility functions
 * Provides stubs for functions that exist in desktop apps but not in library
 */

#include "jabcode.h"
#include <stdio.h>
#include <stdarg.h>

// Note: reportError is defined in encoder.c, no need to duplicate here
