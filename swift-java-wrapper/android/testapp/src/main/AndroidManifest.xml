<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    
    <application
        android:allowBackup="true"
        android:label="JABCode Scanner"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".ScannerActivity"
            android:screenOrientation="portrait"
            android:exported="false" />
        
        <activity
            android:name=".SettingsActivity"
            android:label="Settings"
            android:parentActivityName=".ScannerActivity"
            android:exported="false" />
        
        <activity
            android:name=".calibration.CalibrationActivity"
            android:label="Color Calibration"
            android:exported="false" />
        
        <activity
            android:name=".calibration.ProfileManagerActivity"
            android:label="Calibration Profiles"
            android:exported="false" />

        <!--
            WS-0/2/3 verification harness — Android-ABI all-Nc encode↔decode
            self-test. Secondary launcher: shows its own icon labelled
            "JABCode All-Nc Self-Test" in the app drawer. Runs an in-memory
            roundtrip for Nc=0..7 with no camera dependency. See:
            AllNcSelfTestActivity.java and
            docs/jabcode-all-nc-plan/00-CHECKLIST.md items 0.11, 2.4, 3.11.
        -->
        <activity
            android:name=".AllNcSelfTestActivity"
            android:label="JABCode All-Nc Self-Test"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.jabcode.test.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
