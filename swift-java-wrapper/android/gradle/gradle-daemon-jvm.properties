#This file is generated by updateDaemonJvm
# Run the Gradle daemon on a JDK 17 toolchain. AGP 8.x requires the daemon to
# run on JDK 17+, but the system default here is JDK 23 (which Gradle 8.9 cannot
# run on) — so the daemon JVM is selected by toolchain version, not a hardcoded
# path. Gradle discovers an installed JDK 17 (this repo's contributors have one
# in a standard location; CI provides one via actions/setup-java). Regenerate
# with: ./gradlew updateDaemonJvm --jvm-version=17
toolchainVersion=17
