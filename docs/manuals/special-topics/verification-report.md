# JC-S Special Topics — Stage 4 Verification Report

**Book:** jabcode Special Topics: Math, Logic and Game Theory (11 chapters, three parallel Writers)
**Verifier:** fresh-eyes pass, 2026-07-16, per `manual-forge` verification protocol
**Ground truth used:** direct file view of fork @ `8f76559` (file tools only — the session shell exposes a different clone); ISO/IEC 23634:2022 extract (`JABCodeCOA-crypto/.amazonq/rules/memory-bank/documentation/specification/ISO-IEC-23634.txt`); JC-T findings register as verified input; independent numeric re-derivation (Python + a compiled C replica of `pseudo_random.c` for the PRNG chain).

## Verdict

**Conditional pass.** Ten of eleven chapters pass all deterministic and mathematical checks outright — every worked computation re-derived for chapters 2, 3, 4, 5, 6, 7, 8, 9, 10 reproduces, and roughly 90 source/ISO anchors spot-verified match, most verbatim. The single blocker is **chapter 11**, which misquotes three interop-critical constants from `encoder.h:226/234` (defects D1–D3 below) — a deterministic-check failure under protocol §1 ("mismatch = section fails"). The fixes are one-line and fully specified; the chapter's game-theoretic structure, scope discipline, and equilibrium arithmetic are otherwise sound. Ship after D1–D3 are corrected.

---

## 1. Deterministic checks

### 1.1 Source anchors (spot-verified ≥ 4 per chapter; ~90 total)

All of the following were read at the cited lines and match the book's claims, including verbatim quotes:

- **Ch. 1:** `ecclevel2coderate[11]`/`ecclevel2wcwr[10][2]`/`wcwr_for_level` (encoder.h:226/234/241); `Pn` as local at ldpc.c:651 and :910 with the misaligned declaration comment at :648 (comment order gives Pn "number of parity check symbols" while the assignment is `Pn=data->length` — book's reading correct); "subtract pivot row GF(2)" (ldpc.c:318); `zero_lines_nb`/`swap_col` (ldpc.c:269/278); G-matrix comment "remember matrixA is now A = \[I CT\]..." (ldpc.c:482-483); MSB-first bit packing (ldpc.c:203).
- **Ch. 2:** `getSymbolCapacity` term-for-term (encoder.c:651-689: FP 4×17/4×7, 64-colour palette cap :664, `jab_ap_num` census :668-670, `round(log/log)` :672, metadata ceiling + 4 Part I modules :676-685); `isDefaultMode` :591-598 (colour==8 ∧ ecc∈{0,3} only — shape not tested); `getOptimalECC` :698-714 with wc 3..8, wr wc+1..9 and the verbatim comment "max_gross_payload = floor(capacity / wr) * wr" (:705).
- **Ch. 3:** `createMatrixA` band + permuted stacks under `LPDC_MESSAGE_SEED` (ldpc.c:199-220); `createMetadataMatrixA` (m = n/2, seed 38545, `nb_once` arithmetic, :430-466); `decodeMessage` (accumulate-f, flip-all-ties, under-36 deterministic-LCG tie-break :855-867, `prev_index` exclusion, syndrome verdict, :770-896); `decodeLDPChd` wr≤3 branch `wc=2; if(Pn>36) wc=3` (:916-922); max_iter=25 at both :909 and :1379; `decodeMessageILL` tanh/atanh (:1139-1151), empirical variance (:1108-1125), λ<0 decision (:1162-1165 — the Annex B sign discrepancy is real); memoization determinism comment (:515-519).
- **Ch. 4:** `nc_color_encode_table` (encoder.h:124, no {6,6} row); `setDefaultPalette` 4-colour order K,M,Y,C (encoder.c:104-110); K/C/Y guaranteed in `genColorPalette` output (encoder.c:29-88); Part I placement with mod-reduction and RGB-search branches + the "FIX" comment (encoder.c:1405-1440); Path β rationale and remap (decoder.c:98-132, 1341-1345); the (Y,Y) falsification postscript (:1332-1340); Mode 0 short-circuit (:1307-1318); {0,3,6} acceptance (:1381-1384); pair sentinel 8 (:1242-1250); `decodeLDPChd(part1, 6, 2, 0)` (:1434); metadata geometry (decoder.h:20-25).
- **Ch. 5:** `interleaveData`/`deinterleaveData` exactly as described (single seed at entry :28/:55, one draw per position, swap partners `pos` and `length−1−i`, PRNG-agnostic scatter inversion); PRNG core and `_Thread_local` (pseudo_random.c:10-30); `pn_index` contract quoted verbatim (pseudo_random.h:13-38); "PRNG is NOT an axis" quoted verbatim (encoder.h:23-24); call sites encoder.c:2387 / decoder.c:1967.
- **Ch. 6:** W1/W2/W3 (mask.c:22-24); commented-out per-row/column rule 1 (:67-129) vs live cross-only (:130-183); rule 2 as 2×2 windows (:192-210); rule 3 fires at `>= 5` (:248-263); all eight generators cell-identical to Table 22 (:315-341); data-map gating and non-data canvas copy (:313, :349-350); `maskCode` strict-< selection from 10000 (:363-399); demask = same XOR (:423-448); Part II re-encode loop (encoder.c:2413-2430).
- **Ch. 7:** `analyzeInputData` structures exactly as described (value table `(i+1)*14+j`, backpointers, `switch_mode`/`temp_switch_mode`, byte fold :372, 14×14 min-update :374-392, shift-back block :394-432, latch-U-only init :342, ENC_MAX/2 bookkeeping init :309-332, argmin + backtrack :456-576, byte-run corrections inside the backtrack :478-524 with "2^13+15" :497); `latch_shift_to` rows 7-13 duplicate 0-6; `character_size` = E.1; all cited `mode_switch` tokens (28/29/13/127/478/60/8188/8189) present at the claimed widths; Annex-D character values verified in `jab_enconing_table` including `[54][2] = 7` for '6' and `jab_decoding_table_numeric` (decoder.h:52) confirming the +1 offset.
- **Ch. 8:** `jab_default_palette` = cube vertices (encoder.h:26-34); `getNearestPalette` spatial proximity (decoder.c:670-697); `decodeModuleHD` black guard :741, `use_direct_rgb` = color_number>8 :750, max-channel normalization :795-798, squared-Euclidean scan :818, B/W disambiguation :837-853, 8-colour-only magenta rescue :855-869, dormant `USE_LAB_DISTANCE` ΔE2000 path :752-792; `LAB_EPSILON` (6/29)³ (lab_color.c:27), `lab_f` :63, `rgb_to_xyz` :90, `delta_e_76` :224, `delta_e_2000` :239; k-d tree median split cycling L/a/b (kdtree_color.c:38-66), ΔE76 search :117, hyperplane pruning :139.
- **Ch. 9:** `checkPatternCross` acceptance region exactly as formalized (mean of 3 inner runs, half-module band :138, outer floor `> 0.5 * layer_tolerance`, |s1−s3| check, comment "n-1-1-1-m" :141); `checkModuleSize2/3` mean/2.5 (:162/:180); `saveFinderPattern` running-mean merge + vote (:1143-1167); `selectBestPatterns` with both suppression quotes verbatim (:1323-1325, :1381-1402); missing-FP scale-corrected parallelogram solve — code form `(p3−p2)/m̄23 × m̄13 + p1` matches the book's formula exactly (:2082-2093), image-bounds gate (:2131-2138), `seekMissingFinderPattern` (:2144), two-missing failure "Too few finder pattern found" (:2070-2075); `square2Quad` affine branch :46-57 and 2×2 perspective solve :60-66, adjugate inverse :105-113, composition :173-183, 3.5-module corners :208-211, `warpPoints` :225; `getSideSize` mod-4 table with verbatim comment :3009 and the \[21,145\] clamp :3013-3022; `chooseSideSize` bigger-flag/max merge :3034-3048; `calculateModuleNumber` ⌊dist/mean + 0.5⌋ with cos θ = max(|Δx|,|Δy|)/dist :3057-3065; +7 in `calculateSideSize` :3082-3092; Mode 0 quadrant typing :1999-2009; vertical rescan condition :2042-2047; `min_module_size` forced 1 under `INTENSIVE_DETECT` :1814-1815.
- **Ch. 10:** `jab_symbol_pos` (encoder.h:111-119) — all 61 entries confirmed as the closed L1 ball of radius 5, and the enumeration hand-checked against BFS (top-bottom-left-right) through ring 3 plus the last entry (5,0) at index 60; `assignDockedSymbols` (encoder.c:1598-1677): host=−1 init, side order, top test `y_host − 1` at :1625, array-as-queue via `swap_symbols`, hostless failure with the exact message; `MAX_SYMBOL_NUMBER 61`; exercise-1 offsets (index 1/4/6/12) all correct.
- **Ch. 11:** instrument anchors (encoder.h:226/234, mask.c:22, interleave.c, detector.c:2078-2145, encoder.c:1598) resolve; but see D1–D3.

### 1.2 ISO-anchored quotes vs the extract (cell-by-cell where quoted)

- **Table 20** (lines 1748-1761): all ten rows — recovery %, wc, wr, printed R — match ch. 3's reproduction exactly, decimal commas included. Qualifier sentence verbatim.
- **Table 29 / 8.2.2** (2788-2833): Ecap 8, 9, 10, 11, 14, 17, 22, 24, 26, 29; UEC formula `1 - (e + 2*t)/(Pg × Ecap)`; grade bands — all match chs. 3/11.
- **Table 7 / 4.4.1.2** (968-1021): all 8 rows match ch. 4's table; (yellow, yellow) is the unique absent pair; the three-colour-mode sentence is quoted verbatim; the placement-flow sequence (Part I → palettes → Part II/message/Part III) is present as quoted.
- **Table 1 / 4.3.5** (597-701): all seven 8-colour rows the book reproduces (SV 1, 5, 10, 16, 20, 26, 32) match in data modules and bits; the 4-colour column sits exactly 11 modules below the 8-colour column at every printed version — the book's uniform-gap finding is real. The 4.3.5 formulas (DFCB=4, MDBA=16, ax, a, CPalette, FPrimary/FSecondary, the flattened Metadata formula) are exactly as the book renders them. Additional corroboration the book did not use: **4.4.4/Table 12** (1216-1236) states Part I = 4 modules + Part II = 19 modules = **23** metadata modules at 4 colours — the spec's own module accounting agrees with the source arithmetic and contradicts Table 1's implied 27.
- **Annex D** (3323-3391): message, 78 bits, "Pg = 1071 and K = 476", D.3's "metadata length is 10 modules" vs D.4's "no metadata needs to be encoded" — all as the book states. Table D.1 prints '6' → Numeric value **5**, binary "0101" (3355) — confirming ch. 7's flag; note the same table's '0'→1, '1'→2, '2'→3 rows imply the +1 offset under which '6' must be 7, so the printed 5 is internally inconsistent within Table D.1 itself.
- **Annex E** (3405-3496): Tables E.1/E.2/E.3 extracted whole and compared cell-by-cell against `latch_shift_to[14][14]`. Result: agreement in every cell **except exactly the six** the book lists (L→N shift 7 vs ENC_MAX; N→A latch 106 vs 9; N→B shift 106 vs 10; A→L latch 106 vs 13; A→N latch 106 vs 13; B→B shift 106 vs 0). No seventh divergence; none of the book's six is spurious. Step 1's dual-zero initialization printed as the book quotes; the trellis sentence verbatim.
- **Annex F** (3510-3534): steps a)–f) and the C routine verbatim as quoted in ch. 5; clause 5.5 (1843-1849) fixes seed 226759, clause 5.4.4 (1835-1837) fixes 785465/38545 — see D6.
- **Annex B/C** (3147-3310): "wc ≥ 3 and wr ≥ wc+1"; dmin/(dmin−1)/2; soft decoder assigned to metadata with λ>0 tentative decision and L=25; hard decoder to message data ("flips those bits with the maximum λ..."); Annex C normative, "wc = 2, if the metadata length is shorter than 36 bits, otherwise wc = 3" (so exactly-36 → 3, as ch. 3's boundary analysis requires); the flattened row-count formula; c = m ⊗ G stated in Annex C. All match.
- **Annex G** (3548-3693): G.1 a) quoted verbatim (ch. 8); G.3 tier ladders for 16/32/64/128/256 all match ch. 8's table; the 64-colour embedded-palette cap sentence and the tier-36 interpolation example verbatim; embedded subsets R∈{0,73,182,255} (128) and R,G∈{0,73,182,255} (256) as stated.
- **Clause 8.3.1/8.3.2** (2976-3038): "half the distance to the next colour in this colour channel (see Annex G.1)" verbatim; grade bands including the "5,6 – round(80 * CAP)" quirk (spec writes both CPA and CAP) and the "down to the next 0,1" rounding note — as ch. 8 quotes.
- **Table 24 / Formulas (5)-(8)** (2257-2325): floor(... + 7.5) form and the mod-4/flag procedure match ch. 9 exactly; Table 24 indeed has no range clamp (the source's \[21,145\] rejection is an addition, as the book says); the bigger-flag merge is also spec text (2311).
- **Clause 4.5/4.5.2/Annex A.3** (1294-1343, 3081-3094): layer procedure with the exact skip examples (5,6,7 / 8,9,10 / fourth-layer 6,9,11) as ch. 10 restates; "The indices of the first 60 secondary symbols are defined in Figure 14"; "at least 5 pixels per module"; primary-largest recommendation; A.3's lower-overhead and "may consequently decrease decoding reliability" caveat — all verbatim.
- **Tables 22/23 / 5.8** (1925-2002): all eight generators cell-identical to ch. 6's table; W1=100/W2=3/W3=3; rule 2 `W2 × (m – 1) × (n – 1)`; rule 3 `(5+k), k > 0 → W3 + k`; the XOR sentence and the joint-evaluation sentence verbatim; "(x, y) = (0, 0) for the upper left module".
- **Clause 6.1 step e)** (2039-2041): invalid Part I → default metadata values, as ch. 4 states.

### 1.3 Links, lint, math fences

- All relative links resolve: siblings, `../corpus-model.md`, `../developers-manual/{03,04,05,06,07,08,10,14,15,16}-*.md`, `../developers-manual/index.md`, `../operators-manual/{index,02-capacity-size-robustness}.md`. No broken links found.
- `index.md` was **missing** (deterministic requirement: the book's index lists every chapter). Created by this pass — see fix log F4.
- CommonMark: headers followed by blank lines, lists preceded by blank lines, `<details>` folds well-formed — spot-checked across all chapters, no violations found.
- Math: display math uses `$$` fences exclusively; no inline single-`$` math found (regex sweep). Escaping contract honored (`\[ \]`, `\{ \}`, literal `\$` not needed anywhere; ISO decimal commas reproduced and tagged).

## 2. Math verification (independent re-derivation)

| # | Item | Result |
|---|---|---|
| M1 | Ch. 2 capacity ledger, all seven table rows (SV1/5/10/16/20/26/32, 8-colour): side, a, palette, finder, data modules, bits | **Reproduces exactly**, including a-column ↔ `jab_ap_num` agreement (ax+2 = census at every version) |
| M2 | Ch. 2 rate toll: Pg=1044, K=464, Pn=580 (SV1); Pg=61182 (exact ×9), K=27192, Pn=33990 ≈ 4.2 kB; 7647.75 bytes headline | **Correct** |
| M3 | Ch. 2 4-colour discrepancy: 11-module uniform gap ⇒ M=27 vs source M=4+⌈38/2⌉=23 | **Correct**, and independently corroborated by spec 4.4.4/Table 12 (= 23) |
| M4 | Ch. 2 Annex D audit: 1071 = 9×119 = 3×357; 476 = 1071×4/9; 357 = 441−68−16 (palette-16 as unique single-term candidate); ex. 3's 441−68−16−10 = 347 ≠ 357 | **All arithmetic correct** |
| M5 | Ch. 2 exercises: SV5 4-colour (1270/2540 vs printed 1266/2532); rectangle (2,1): 525, 433, 1299, 1296/576/720 | **Correct** |
| M6 | Ch. 3 rate identity, Table 20 1−wc/wr column, separation lemma (P_odd closed form, wc(1±δ)/2), descent identity ΔΦ = wc−2f | **Correct** |
| M7 | Ch. 3 ex. 2 (K5 code): wc=2/wr=4, one-iteration correction, rank 4 via cut argument, R = 0.6 | **Correct** |
| M8 | Ch. 3 ex. 1 solution's rounding classification | **Was wrong for level 9** (0,17 is rounded up from 0.1667, not truncated) and omitted level 5 — fixed (F1); the "no single rule" conclusion survives |
| M9 | Ch. 4: 255√2 equilateral triangle; 2log₂3 ≈ 3.17; 8-of-9 codewords; (Y,Y) unique absent pair; {0,3,6} mod 4 = {0,3,2}; ex. 1 bit-burst counts (2-bit errors for cases b/d) | **Correct** |
| M10 | Ch. 5 Fisher-Yates uniformity proof (draw-vector bijection, backward reconstruction) | **Sound** |
| M11 | Ch. 5 N=4 seed-226759 identity claim: full re-simulation of setSeed(226759) → lcg64_temper (MMIX multiplier, MT tempering) → pn_index (float semantics emulated, then confirmed with compiled C) | **Identity permutation confirmed**; pn_index chain 3,2,1,0 (all self-swaps); §5.3's mod-vs-scaling example (3 vs 6 on draw 1) confirmed. **But the printed third draw was wrong**: true value 3982877425, book printed 3982681841 (draws 1, 2, 4 exact) — fixed (F2); no downstream value changes |
| M12 | Ch. 5 ex. 2: C89 rand() from seed 1 → 16838, 5758; swaps (6,7) then (4,6). Ex. 1(b): rand() from 226759 → 11845, 16399, 13855, 21175; resulting 3-cycle | **All confirmed by simulation** |
| M13 | Ch. 6 ex. 1 penalties: mask 000 → 0 (checked incl. the (4,4) wrap to 0 — no adjacent equals); mask 001 → 5×(W3+0)=15; spec-literal rescore → 0-0 tie resolved to reference 0 | **Correct** |
| M14 | Ch. 7 DP table on "JAB Co": every cell re-derived from `latch_shift_to`/`character_size`/`jab_enconing_table` (U:5,10,15,20,25; A:11,16,21,26,31,36; L:25→35; N:24 at SP); final 35; full ledger 25+5+15+5+20+4+4 = 78 | **Reproduces exactly**, including the N=24 greedy near-miss |
| M15 | Ch. 7 six divergences + token widths (127→7 bits, 478→9, 60→6+4, 8188/8189→13, dead-token and optimality-gap analyses: 5+4+6=15 vs 7+4=11) | **All verified against encoder.h and the E.2/E.3 extract** |
| M16 | Ch. 7 ex. 1 ("A1"): U=5/A=11; layer 2 N=14, A=16, byte-fold 24; stream 00001·11101·0010 = 14 bits | **Correct** |
| M17 | Ch. 8: d(P₈) ∈ {255, 255√2, 255√3} with 12/12/4 pairs; 8-tier gaps 36/37 alternation, min 36; half-gaps 127.5/42.5/18; top-grade slacks 25.5/8.5/3.6; embedded subset = tiers {0,2,5,7}, gaps 73/109/73; 36.5 vs 36 midpoint bias; ex. 1 (12 pairs at 85); ex. 2 bound 73²/8 ≈ 666, threshold ≈ 4.7×10⁻³, 109²/8 ≈ 1485 | **All correct except the frontier summary sentence** — see D5 |
| M18 | Ch. 9: ⌊x+0.5⌋+7 = ⌊x+7.5⌋ equivalence; mod-4 snap table semantics incl. upward-only residue-3 guess; mean/2.5 = 40 % band | **Correct** |
| M19 | Ch. 10: \|B₁(5)\| = 1+4+8+12+16+20 = 61; BFS enumeration equals `jab_symbol_pos` (hand-checked through ring 3 + spec's fourth-layer example + terminal (5,0)); BFS proof (termination/no-repeat/completeness/index agreement) | **Sound**; ex. 1 decode indices and offsets correct |
| M20 | Ch. 11 toy game: α\* = 0.5·0.05/0.06 ≈ 0.417; route B needs (1−α) ≥ 0.095/0.06 ≈ 1.58 (infeasible); tightened kill 0.75 → α\* = 0.625; ex. 1: 0.2375/0.06 ≈ 3.96 | **Equilibrium arithmetic correct** given the stated toy parameters; but see D1–D3 for the misquoted real constants framing it |

## 3. Fade schedule and inter-chapter consistency

- **First-occurrence assignments honored:** GF(2)/systematic-form derived fully in ch. 1 (ch. 3 explicitly sketches and cites back); bit-flipping fully derived in ch. 3 with the soft decoder explicitly faded ("second appearance of iterative decoding"); Fisher-Yates fully proved in ch. 5 (ch. 1 and ch. 3 both defer to it by name); the Bellman/DP derivation fully in ch. 7 ("the book's first DP"); homography sketched in ch. 9 as "second geometry occurrence" after ch. 8; cascade traversal proof at exercise grade in ch. 10 as the third traversal occurrence. No later chapter re-derives a faded family.
- **JC-T consistency:** ch. 7's verdict ("implements Annex E's 14-state trellis DP... cost table deviates in six cells, plus post-hoc byte-run corrections") is consistent with and strictly sharpens JC-T §4.4.2's one-line characterization; no contradiction. Ch. 2's audit matches JC-T §4.4.3's open item and adds the unique-single-term analysis. Ch. 3's λ-sign and caller-less findings match JC-T ch. 6. Ch. 5 matches JC-T ch. 8 (divergence framing, `pn_index` contract, draw-count-is-state). Ch. 6 matches JC-T ch. 7 (both divergences, wire-safety argument, default-mode skip). Chs. 8/9/10 match JC-T chs. 10/5/4 gate statuses and findings.
- **Writer-seam note (resolved):** chapters 8-11 lacked the `<!-- objective: -->` annotations chapters 1-7 carry; added from the outline (F3). Ch. 11 also reads level constants differently from ch. 3's verified table — that is defect D1-D3, not a style seam.

## 4. Original-synthesis discipline

- Ch. 11's scope note is present, prominent (first section), and accurate; toy parameters (c = 0,10; p = 0,06; f = 0,05; kill = 0,5) are all stated at point of use and labeled illustrative; the crypto-layer game is explicitly deferred to AF-S. **Pass.**
- Ch. 6 §6.5 design-space argument labeled "this book's analysis, labeled as such". Ch. 8 vertex-optimality labeled "design rationale, not theorem". Ch. 4's equilateral-triangle observation labeled analytical. Ch. 7's sentinel bound labeled "this book's". **Pass.**
- No invented empirical claims found: every empirical number in the book (77/78, 34/34, 91 % white, 2700-bit sub-blocks, etc.) traces to a source comment or spec sentence that was verified.

## 5. Objective (ABCD) and Diátaxis checks

All eleven chapters classify as **explanation** (cognition × study); exercises are pedagogic devices, not procedures; no tutorial/how-to/reference drift found. Per-chapter objective check: each chapter's declared behavior is achievable from the chapter plus declared prerequisites — ch. 1 (map/state: symbol table + conventions delivered), ch. 2 (derive/evaluate: ledger + audit), ch. 3 (derive/analyze/evaluate), ch. 4 (explain/analyze), ch. 5 (prove/explain/evaluate), ch. 6 (formulate/explain/evaluate), ch. 7 (formulate/derive/analyze), ch. 8 (analyze/derive/evaluate), ch. 9 (model/explain/derive), ch. 10 (characterize/prove/analyze), ch. 11 (construct/analyze/evaluate — objective met once D1-D3 are fixed, since the evaluation instrument depends on quoting the rate ladder correctly).

## 6. Defect register

| ID | Ch. | Severity | Defect | Disposition |
|---|---|---|---|---|
| D1 | 11 | **Major (blocker)** | "level 1 → 0.55" (§Two channels): `ecclevel2coderate` level 1 is **0.63**; 0.55 is the index-0 placeholder. Contradicts ch. 3's verified table. | Writer fix: "level 1 → 0.63". One line. |
| D2 | 11 | **Major (blocker)** | "level 9 (R = 0.20)" (§instrument 1): level 9 is **0.17** (0.20 is level 8). The companion claim "roughly triples the parity" fits no reading (fixed-Pg parity fraction 0.45→0.83 ≈ ×1.8; fixed-Pn parity ≈ ×6). | Writer fix: correct the rate and re-word the multiplier (e.g. "nearly doubles the parity fraction (0.45 → 0.83)"). |
| D3 | 11 | **Major (blocker)** | "(wc, wr) = (3, 6)" attributed to level 3 (§toy game): level 3 is **(4, 9)**; (3, 6) is level 4. | Writer fix: "(4, 9)". One token. |
| D4 | 4 | Minor (finding upgrade) | §4.3 marks "whether this ordering reproduces the spec's Table 3 for 4-colour mode" as NOT FOUND ("Table 3 was not pulled"). The extract's **Table 21** (lines 1904-1907) does define the 4-colour index order — black 0, **cyan 1, magenta 2**, yellow 3 — which **differs** from the code's K, M, Y, C (encoder.c:104-110). The hedge was honest but the evidence existed; the underlying spec-vs-reference 4-colour palette-order divergence is a real, wire-relevant finding not currently in the JC-T register. | Writer: replace the NOT-FOUND sentence with the Table 21 citation and the divergence statement. Also propose as a new JC-T register entry (4-colour data-encoding order: spec Table 21 K,C,M,Y vs reference K,M,Y,C — interchange consequence parallels the PRNG divergence pattern). |
| D5 | 8 | Minor | "Each ~1.4 bits of density costs a factor of ~2.7 in margin" is not derivable from the frontier table: a line fit over (3, 127.5) → (8, 18) gives ≈ 2.5 bits per ×2.7 (equivalently ×1.48 margin loss per bit); 1.4 ≈ log₂ 2.7 suggests a units slip (halvings vs factor). | Writer fix: e.g. "each single bit of density costs a factor of ≈ 1.5 in margin on the fitted frontier — a factor of ≈ 2.7 every two-to-three bits". |
| D6 | 5 | Minor | §5.4's curiosity paragraph calls the seed "unspecified": Annex F's sample code carries no numeric seed, but clauses 5.5 and 5.4.4 (normative text the chapter itself cites elsewhere) fix 226759/785465/38545. Only the reduction and the generator's informative status are genuinely underdetermined. | Writer fix: "the seed (fixed not by the annex but by clauses 5.5/5.4.4)" or equivalent. |
| D7 | 7 | Nit | "every token Annex D exercises, this table reproduces" (one-sided verifiability paragraph) reads as all-token when the '6'→5 character value, flagged three paragraphs earlier, does not reproduce. Context (switch tokens) makes the intent clear. | Optional writer re-word: "every **switch** token". |
| D8 | 7/JC-T | Note (cross-book) | Verification of the flagged '6' token: the extract's Table D.1 does print value 5 **and** binary "0101" (consistent with each other, inconsistent with the same table's '0'→1/'1'→2/'2'→3 offset). So the anomaly is inside the ISO text, not the extraction; JC-T §4.4.3's "the source tables reproduce every token of that sequence" is slightly over-claimed. | Follow-up item for the JC-T register; JC-S's hedged wording may stand or cite this report. |
| D9 | book | Resolved | No `index.md` (deterministic requirement). | Created this pass (F4). Note JC-T's index still says "JC-S, forthcoming" — one-line JC-T touch-up when convenient. |

## 7. Mechanical fixes applied by the Verifier (logged)

| ID | File | Change |
|---|---|---|
| F1 | 03-ldpc-coding-theory.md | Ex. 1 **solution**: corrected the printed-R rounding classification (level 9 is rounded up, not truncated; level 5 added; levels 2/10 marked rule-ambiguous). Conclusion unchanged. |
| F2 | 05-interleaving-and-determinism.md | Ex. 1: corrected the third PRNG draw 3982681841 → **3982877425** and the solution's ratio 0.9272… → 0.9273… (proven by Python emulation and a compiled C replica of `pseudo_random.c`; draws 1, 2, 4 and every derived index/permutation unchanged — the identity-permutation claim stands). |
| F3 | 08/09/10/11-*.md | Added the `<!-- objective: -->` annotations (from the approved outline) that chapters 1-7 carry, for cross-book consistency and audit. |
| F4 | index.md | Created the book index (all 11 chapters, voice/corpus header, conventions), satisfying protocol §1. |

## 8. Cold-read triad scores

| Cluster | Chapters | Completeness | Truthfulness | Helpfulness | Notes |
|---|---|---|---|---|---|
| I | 1-3 | 9 | 9 | 9 | Symbol table honest and re-verified; ledger reproduces Table 1 bit-for-bit; Annex D audit is exemplary Evaluate-level work; one solution slip (fixed). |
| II | 4-7 | 9 | 9 | 9 | Ch. 7's six-cell diff independently reproduced cell-by-cell — the book's strongest chapter; ch. 5's determinism analysis verified to the draw; ch. 4 dinged only by D4. |
| III | 8-11 | 9 | 7 | 8 | Chs. 8-10 verify near-perfectly (D5 is one sentence); ch. 11's three misquoted constants (D1-D3) are deterministic-check failures in an otherwise disciplined, well-scoped synthesis chapter. Truthfulness recovers to 9 once fixed. |

**Ship condition:** apply D1-D3 (three one-line edits in ch. 11); D4-D6 recommended in the same pass; D7-D9 optional/follow-up.
